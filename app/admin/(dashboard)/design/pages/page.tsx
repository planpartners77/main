import { DesignPlaceholder } from "@/components/admin/design/DesignPlaceholder";

export default function DesignPagesPage() {
  return (
    <DesignPlaceholder
      title="페이지 관리"
      description="메인페이지 섹션(히어로·후기·인기상품 등)의 노출 여부·순서·문구를 편집합니다."
    />
  );
}
